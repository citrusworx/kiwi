user:
  query001:
    type: select
    table: users
    fields:
     id
     name
    conditions:
      column: age
      operator: '>'
      value: 30
  query002:
      type: insert
      table: users
      fields:
        id
        name
      values:
        id
        name